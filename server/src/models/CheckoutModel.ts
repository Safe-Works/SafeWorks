type CheckoutModel = {
    id: string,
    title: string,
    price: number,
    quantity: number,
    description: Date,
    accessToken: string,
    picture_url: string,
}

export default CheckoutModel;